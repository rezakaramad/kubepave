{{- if false }}{{/* TenantRequest Composition disabled — replaced by TenantClaim pipeline. Source kept for reference. */}}

apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: tenantrequest-default
  labels:
    crossplane.io/xrd: tenantrequests.idp.rezakara.demo

spec:
  compositeTypeRef:
    apiVersion: idp.rezakara.demo/v1alpha1
    kind: TenantRequest

  mode: Pipeline

  pipeline:
    - step: render
      functionRef:
        name: function-tenant-validator

---
{{- end }}
