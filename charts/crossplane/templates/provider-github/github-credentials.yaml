{{- if .Values.githubProvider.enabled }}
# This will allow Crossplane to create deployment repositories in 'talktorubberduckdev' GitHub organization.

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: github-org-rezakaramad
  namespace: crossplane-system
spec:
  refreshInterval: 20m
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: SecretStore
  target:
    name: github-org-rezakaramad
    creationPolicy: Owner
    template:
      engineVersion: v2
      type: Opaque
      mergePolicy: Replace
      data:
        credentials: |
          {
            "app_auth": [{
              "id": "{{`{{ .app_id }}`}}",
              "installation_id": "{{`{{ .installation_id }}`}}",
              "pem_file": {{`{{ .pem | toJson }}`}}
            }],
            "owner": "rezakaramad"
          }
  data:
    - secretKey: app_id
      remoteRef:
        key: crossplane/github/apps/org-rezakaramad
        property: app-id
    - secretKey: installation_id
      remoteRef:
        key: crossplane/github/apps/org-rezakaramad
        property: installation-id
    - secretKey: pem
      remoteRef:
        key: crossplane/github/apps/org-rezakaramad
        property: private-key

---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: github-org-talktorubberduckdev
  namespace: crossplane-system
spec:
  refreshInterval: 20m
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: SecretStore
  target:
    name: github-org-talktorubberduckdev
    creationPolicy: Owner
    template:
      engineVersion: v2
      type: Opaque
      mergePolicy: Replace
      data:
        credentials: |
          {
            "app_auth": [{
              "id": "{{`{{ .app_id }}`}}",
              "installation_id": "{{`{{ .installation_id }}`}}",
              "pem_file": {{`{{ .pem | toJson }}`}}
            }],
            "owner": "talktorubberduckdev"
          }
  data:
    - secretKey: app_id
      remoteRef:
        key: crossplane/github/apps/org-talktorubberduckdev
        property: app-id
    - secretKey: installation_id
      remoteRef:
        key: crossplane/github/apps/org-talktorubberduckdev
        property: installation-id
    - secretKey: pem
      remoteRef:
        key: crossplane/github/apps/org-talktorubberduckdev
        property: private-key

---
{{- end }}
