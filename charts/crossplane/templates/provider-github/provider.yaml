{{- if .Capabilities.APIVersions.Has "pkg.crossplane.io/v1/Provider" }}
{{- if .Values.githubProvider.enabled }}

apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: github-talktorubberduckdev
  namespace: crossplane-system
spec:
  package: {{ .Values.githubProvider.providerPackage }}

---
{{- end }}
{{- end }}
