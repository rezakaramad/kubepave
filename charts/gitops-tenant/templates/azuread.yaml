{{- range $cluster := $.Values.workloadClusters }}
{{- range $role := $.Values.tenant.rbac.roles }}

{{- $assignment := $role.entraId.assignment | default dict }}
{{- $selectorEnabled := and $assignment.principalObjectIdSelector $assignment.principalObjectIdSelector.enabled }}
{{- $principals := $assignment.principalObjectIds | default list }}

# --------------------------------------------------
# Group (Enterprise only)
# --------------------------------------------------
{{- if and (not $.Values.azure.freeTier) $selectorEnabled }}

apiVersion: groups.azuread.m.upbound.io/v1beta1
kind: Group
metadata:
  labels:
    prefix: {{ $cluster.environmentPrefix }}
    role: {{ $role.name }}
    tenant: {{ $.Values.tenant.name }}
  name: {{ $.Values.tenant.name }}-{{ $role.name }}-{{ $cluster.environmentPrefix }}
  namespace: crossplane
spec:
  forProvider:
    displayName: {{ $.Values.tenant.name | title }}-{{ $role.name | title }}-{{ $cluster.environmentPrefix | title }}
    securityEnabled: true
  providerConfigRef:
    name: azuread
    kind: ProviderConfig

---
{{- end }}

# --------------------------------------------------
# AppRole
# --------------------------------------------------
{{- if not $role.entraId.appRoleUUID }}
{{- fail (printf "Missing appRoleUUID for role '%s'" $role.name) }}
{{- end }}

apiVersion: applications.azuread.m.upbound.io/v1beta1
kind: AppRole
metadata:
  labels:
    prefix: {{ $cluster.environmentPrefix }}
    role: {{ $role.name }}
    tenant: {{ $.Values.tenant.name }}
  name: {{ $.Values.tenant.name }}-{{ $role.name }}-{{ $cluster.environmentPrefix }}
  namespace: crossplane
spec:
  forProvider:
    roleId: {{ $role.entraId.appRoleUUID | quote }}
    allowedMemberTypes:
      - User
      - Group
    applicationId: {{ $.Values.tenant.rbac.entraAppId | quote }}
    description: "{{ $role.name }} access for {{ $.Values.tenant.name }} in {{ $cluster.environmentPrefix }} environment"
    displayName: "{{ $.Values.tenant.name | title }} {{ $role.name | title }} {{ $cluster.environmentPrefix | title }}"
    value: {{ $.Values.tenant.name }}-{{ $role.name }}-{{ $cluster.environmentPrefix }}
  providerConfigRef:
    name: azuread
    kind: ProviderConfig

---
# --------------------------------------------------
# RoleAssignment
# --------------------------------------------------
# We use selector-based assignment for groups in enterprise tier, and direct principalObjectId assignment for users in free tier.
# In enterprise tier, the RoleAssignment will automatically assign the AppRole to the group that matches the specified labels.

{{- if and (not $.Values.azure.freeTier) $selectorEnabled }}

apiVersion: app.azuread.m.upbound.io/v1beta1
kind: RoleAssignment
metadata:
  name: {{ $.Values.tenant.name }}-{{ $role.name }}-{{ $cluster.environmentPrefix }}
  namespace: crossplane
spec:
  forProvider:
    appRoleId: {{ $role.entraId.appRoleUUID | quote }}
    principalObjectIdSelector:
      matchLabels:
        prefix: {{ $cluster.environmentPrefix }}
        role: {{ $role.name }}
        tenant: {{ $.Values.tenant.name }}
  providerConfigRef:
    name: azuread
    kind: ProviderConfig

---
# In enterprise tier we'll use the selector to assign roles to groups, so we don't need to create individual RoleAssignments for users.
# In free tier, since we can't assign roles to groups, we need to create RoleAssignments for individual users.
{{- else }}

{{- if not $principals }}
{{- fail (printf "No principalObjectIds provided for role '%s'" $role.name) }}
{{- end }}

{{- range $i, $p := $principals }}

apiVersion: app.azuread.m.upbound.io/v1beta1
kind: RoleAssignment
metadata:
  name: {{ $.Values.tenant.name }}-{{ $role.name }}-{{ $cluster.environmentPrefix }}-{{ $i }}
  namespace: crossplane
spec:
  forProvider:
    appRoleId: {{ $role.entraId.appRoleUUID | quote }}
    principalObjectId: {{ $p | quote }}
  providerConfigRef:
    name: azuread
    kind: ProviderConfig

---
{{- end }}

{{- end }}

{{- end }}
{{- end }}
