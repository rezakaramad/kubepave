apiVersion: v2
name: workload-vault-seed
description: >-
  DEPRECATED — superseded by the oidc-proxy chart.
  Previously bootstrapped the vault-reviewer SA and token-review RBAC that
  Vault used to validate workload SA tokens via the Kubernetes auth method.
  Vault now uses JWT auth backed by the oidc-proxy OIDC endpoint instead,
  so no long-lived reviewer token is needed.
deprecated: true
version: 0.0.1
