defaultGateway:
  # The hostname for defeault gateway resource in 'gke-development' cluster
  hostname: '*.dev.gcp.talktorubberduck.dev'

# The Traefik controller use the following static IP to specify the GCP Load Balancer.
traefik:
  service:
    spec:
      loadBalancerIP: "34.179.169.208"
