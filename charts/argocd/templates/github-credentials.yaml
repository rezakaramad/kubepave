apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: argocd-github-identity-rezakaramad
  namespace: argocd
spec:
  refreshInterval: 20m
  secretStoreRef:
    name: vault-local
    kind: ClusterSecretStore
  target:
    name: argocd-github-identity-rezakaramad
    creationPolicy: Owner
    template:
      type: Opaque
      metadata:
        labels:
          argocd.argoproj.io/secret-type: repo-creds
      data:
        url: https://github.com/rezakaramad
      mergePolicy: Merge
  data:
    - secretKey: githubAppID
      remoteRef:
        key: management/github/apps/argocd/rezakaramad
        property: app-id

    - secretKey: githubAppInstallationID
      remoteRef:
        key: management/github/apps/argocd/rezakaramad
        property: installation-id

    - secretKey: githubAppPrivateKey
      remoteRef:
        key: management/github/apps/argocd/rezakaramad
        property: private-key

---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: argocd-github-identity-fluxdojo
  namespace: argocd
spec:
  refreshInterval: 20m
  secretStoreRef:
    name: vault-local
    kind: ClusterSecretStore
  target:
    name: github-identity-fluxdojo
    creationPolicy: Owner
    template:
      type: Opaque
      metadata:
        labels:
          argocd.argoproj.io/secret-type: repo-creds
      data:
        url: https://github.com/fluxdojo
      mergePolicy: Merge
  data:
    - secretKey: githubAppID
      remoteRef:
        key: management/github/apps/argocd/fluxdojo
        property: app-id

    - secretKey: githubAppInstallationID
      remoteRef:
        key: management/github/apps/argocd/fluxdojo
        property: installation-id

    - secretKey: githubAppPrivateKey
      remoteRef:
        key: management/github/apps/argocd/fluxdojo
        property: private-key

---
