# Crossplane

Installs Crossplane and the default providers and configurations required by the platform.

## GitHub Authentication

Crossplane uses the **JYSK Crossplane GCP** GitHub App for authentication.  
Credentials are stored in Google Cloud Secret Manager under the prefix:

`plt-github-app-jysk-crossplane-gcp-*`
