---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: inputs.platform.rezakara.demo
spec:
  group: platform.rezakara.demo
  names:
    categories:
    - crossplane
    kind: Input
    listKind: InputList
    plural: inputs
    singular: input
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: |-
          Input is the configuration passed to this Function from the Composition
          pipeline step. It configures the platform validation settings used by the
          tenant validator.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          dns:
            description: DNS configures DNS validation behavior.
            properties:
              baseDomain:
                description: BaseDomain is the DNS suffix used when validating tenant hostnames.
                minLength: 1
                type: string
            required:
            - baseDomain
            type: object
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          clusters:
            description: Clusters lists the workload clusters and prefixes the platform exposes.
            items:
              description: ClusterInput identifies a workload cluster and its prefix.
              properties:
                name:
                  description: Name is the logical workload cluster name.
                  minLength: 1
                  type: string
                prefix:
                  description: Prefix is the DNS/environment prefix associated with the cluster.
                  minLength: 1
                  type: string
              required:
              - name
              - prefix
              type: object
            minItems: 1
            type: array
        required:
        - clusters
        - dns
        type: object
    served: true
    storage: true