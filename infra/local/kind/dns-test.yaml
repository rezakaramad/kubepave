apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: dns-test
  namespace: platform-system
spec:
  parentRefs:
    - name: default-traefik
  hostnames:
    - test.mgmt.rezakara.demo
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /

